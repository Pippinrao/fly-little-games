/** Unused leftover debug page. Product entry is GameCenter / RunGame. */
@Entry
@Component
struct Index {
  build() {
    Column() {
    }
    .width('100%')
    .height('100%')
    .backgroundColor('#12141A')
  }
}
